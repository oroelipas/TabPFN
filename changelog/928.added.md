Stratified row subsampling for the classifier: when `SUBSAMPLE_SAMPLES` is set, each ensemble member now draws rows that preserve the original class proportions, using a balanced round-robin pool per class to ensure uniform row coverage across estimators.
