Auto-scale `n_estimators` at fit time so every feature is covered by at least one ensemble member. The effective count is exposed as `n_estimators_`; a `UserWarning` is emitted when scaling triggers.
