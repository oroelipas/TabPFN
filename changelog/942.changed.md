Keep the inference cache on the GPU by default when `fit_mode="fit_with_cache"`, avoiding host/device transfers on each predict call. The per-estimator KV caches are reachable via `model.executor_.kv_caches`.
